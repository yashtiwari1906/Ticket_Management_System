import React from 'react';
import ReactDOM from 'react-dom/client';
import AvailRoutes from './Routes'; 
import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(

    <AvailRoutes />
  
);

reportWebVitals();
